{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Permissions for the main dashboard window.",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
